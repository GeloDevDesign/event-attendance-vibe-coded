import { query } from "./_generated/server";

export const getCurrentUser = query({
  args: {},
  handler: async () => {
    return null;
  },
});

export const getUserRole = query({
  args: {},
  handler: async () => {
    return null;
  },
});
