export function formatTimestamp(timestamp: number): string {
  void timestamp;
  return "";
}

export function isWithinRange(
  value: number,
  startsAt: number,
  endsAt: number,
): boolean {
  void value;
  void startsAt;
  void endsAt;
  return false;
}
