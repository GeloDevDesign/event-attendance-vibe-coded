import type { JSX } from "react";

export interface AttendanceStatusBadgeProps {
  isPresent: boolean;
}

export function AttendanceStatusBadge(props: AttendanceStatusBadgeProps): JSX.Element {
  void props;
  return <></>;
}
