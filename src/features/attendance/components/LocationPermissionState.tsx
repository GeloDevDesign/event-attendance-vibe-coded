import type { JSX } from "react";

export interface LocationPermissionStateProps {
  isLoading: boolean;
  error: string | null;
}

export function LocationPermissionState(
  props: LocationPermissionStateProps,
): JSX.Element {
  void props;
  return <></>;
}
