import type { JSX } from "react";
import type { MapAttendee } from "../types/attendance-map.types";

export interface AttendeeCharacterMarkerProps {
  attendee: MapAttendee;
}

export function AttendeeCharacterMarker(
  props: AttendeeCharacterMarkerProps,
): JSX.Element {
  void props;
  return <></>;
}
