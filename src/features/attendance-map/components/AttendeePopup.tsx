import type { JSX } from "react";
import type { MapAttendee } from "../types/attendance-map.types";

export interface AttendeePopupProps {
  attendee: MapAttendee;
}

export function AttendeePopup(props: AttendeePopupProps): JSX.Element {
  void props;
  return <></>;
}
