import type { JSX } from "react";
import type { EventMapLocation } from "../types/attendance-map.types";

export interface EventRadiusCircleProps {
  event: EventMapLocation;
}

export function EventRadiusCircle(props: EventRadiusCircleProps): JSX.Element {
  void props;
  return <></>;
}
